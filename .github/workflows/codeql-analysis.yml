# Reusable workflow for code analysis; to eject, you can replace this file with
# https://github.com/AnomieVision/AnomieVision/blob/main/.github/workflows/codeql-analysis.yml
name: "CodeQL"

on:
  push:
    branches: [master]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [master]
  schedule:
    - cron: "36 7 * * 6"

jobs:
  analyze:
    uses: AnomieVision/AnomieVision/.github/workflows/codeql-analysis.yml@master
